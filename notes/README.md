# notes

- `english_source_current_probe.csv` 是当前新版 `EnglishSource.uasset` 导出的英文源表。
- `original_current_locres_probe.csv` 是当前原版 locres 导出，仅作参考；它和新版 `EnglishSource.uasset` 可能存在 key 复用和文本错位。
- `translation_guidelines.md` 保存翻译风格约束。
- `archive/legacy_20260703/` 保存历史审计文件。
